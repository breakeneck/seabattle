export default class Bot {
    states = []

    shot() {
        // for (let row of this.states) {
        //     for (let col of this.row) {
        //
        //     }
        // }
        let [x, y] = [0, 0];

        return [x, y];
    }

    analyze(shotResult) {
        return shotResult;
    }
}